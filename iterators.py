from functools import reduce

mylist = [1,2,4,5,677,23,87,3]

# newList = list(map((lambda n: n), mylist))
# newList = list(filter((lambda n: n%2==0), mylist))
# finalResult = reduce(lambda c, n: c+n, mylist)
# print(finalResult)

# Filter, Map and Reduce

# def anotherFunction(a):
#     return a

# anotherFunction = lambda a: a
# print(anotherFunction(5))